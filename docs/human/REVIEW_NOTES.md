# Review notes

This branch implements the browser-native human football simulation foundation requested in the master specification. Validate with `npm run human:validate`, `npm run lint`, and `npm run build`.
