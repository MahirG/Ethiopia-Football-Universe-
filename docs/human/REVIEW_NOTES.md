# Review notes

This branch implements the browser-native human football simulation foundation requested in the master specification.

Validate with:

- `npm run human:validate`
- `npm run lint`
- `npm run build`

The production build runs both semantic validators before strict TypeScript and Vite bundling.
